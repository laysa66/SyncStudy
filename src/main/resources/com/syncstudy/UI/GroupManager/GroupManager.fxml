<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.syncstudy.UI.GroupManager.GroupController" stylesheets="@group-styles.css">
   <children>
      <!-- Header -->
      <HBox alignment="CENTER_LEFT" spacing="10.0" styleClass="group-manager-header">
         <children>
            <Label text="User Dashbord" textFill="WHITE">
               <font>
                  <Font name="System Bold" size="16.0" />
               </font>
            </Label>
         </children>
         <padding>
            <Insets bottom="10.0" left="15.0" right="15.0" top="10.0" />
         </padding>
      </HBox>

      <!-- Search Section -->
      <HBox alignment="CENTER_LEFT" spacing="10.0">
         <children>
            <Label text="Recherche:" />
            <TextField fx:id="searchBar" promptText="Group Name..." HBox.hgrow="ALWAYS" />
            <Button onAction="#onSearch" text="Search" />
            <ComboBox fx:id="categoryFilter" promptText="Category" />
            <Button onAction="#onFilter" text="Filter" />
            <Button onAction="#onClear" text="Clear" />
         </children>
         <padding>
            <Insets bottom="10.0" left="15.0" right="15.0" top="10.0" />
         </padding>
      </HBox>

      <!-- Status Label -->
      <HBox>
         <children>
            <Label fx:id="label" text="Loading..." />
         </children>
         <padding>
            <Insets bottom="5.0" left="15.0" right="15.0" top="5.0" />
         </padding>
      </HBox>

      <!-- Groups Table -->
      <TableView fx:id="groupsTable" VBox.vgrow="ALWAYS">
         <columns>
            <TableColumn fx:id="nameColumn" prefWidth="200.0" text="Group Name" />
            <TableColumn fx:id="categoryColumn" prefWidth="120.0" text="Category" />
            <TableColumn fx:id="membersColumn" prefWidth="80.0" text="Members" />
         </columns>
         <VBox.margin>
            <Insets bottom="10.0" left="15.0" right="15.0" />
         </VBox.margin>
      </TableView>

      <!-- Action Buttons -->
      <HBox alignment="CENTER_RIGHT" spacing="10.0">
         <children>
            <Button onAction="#onRefresh" text="Refresh" />
            <Button onAction="#onOpenChat" text="Open Chat" />
            <Button onAction="#onClick" text="Group Details" />
         </children>
         <padding>
            <Insets bottom="15.0" left="15.0" right="15.0" top="10.0" />
         </padding>
      </HBox>
   </children>
</VBox>
